# Lightweight local-testing requirements for gtex_gp.
# Versions pinned to match the cluster vformer_env (singularity overlay).
# For full GPU/torch stack, use the cluster env, not this file.

# --- core scientific stack ---
numpy==2.2.6
pandas==2.2.3
scipy==1.15.3
scikit-learn==1.5.2
statsmodels==0.14.6

# --- plotting ---
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.5.1

# --- config / IO ---
PyYAML==6.0.3
omegaconf==2.3.0
h5py==3.15.1

# --- notebooks ---
nbformat==5.10.4
nbclient==0.10.4
jupytext
ipykernel
ipywidgets==8.1.8

# --- modeling (CPU-only torch for local) ---
# Install torch CPU build separately if needed:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
# torch==2.8.0
# gpytorch==1.15.1
# linear-operator==0.6

# --- dev / test ---
pytest==9.0.2
pytest-cov
